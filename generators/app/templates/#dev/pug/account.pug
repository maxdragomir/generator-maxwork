extends layouts/__main


block vars
  -lotteryName      = '<%= project %>'
  -pageType         = 'account'
  -devMode          = false
  -no2kSize         = false
  -rtl              = <%= rtl %>
  -breadcrumbs      = false
  -styles           = true
  -js               = pageType


block content

  // Content -----------------------------------------------------------------------------------------------------


  // Account
  div(class=`${lotteryName}-account__wrapper`)
    div(class=`${lotteryName}-account__body`)
      +addTestText(`${lotteryName}-test-text`)
  // Account



  // Account Tickets
  div(class=`${lotteryName}-account__wrapper ${lotteryName}-account-logged`)
    div(class=`${lotteryName}-account__body`)
      +addTestText(`${lotteryName}-test-text`)
  // Account Tickets


  // Content -------------------------------------------------------------------------------------------------------





  // Addition Buttons ----------------------------------------------------------------------------------------------
  +addAditions(
  pageType,
  '#index',
  <%_ if (raffle) { -%>
  '#raffle',
  <%_ } -%>
  )
  // Addition Buttons ----------------------------------------------------------------------------------------------
