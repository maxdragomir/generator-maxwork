extends layouts/__main


block vars
  -lotteryName      = '<%= project %>'
  -pageType         = 'account'
  -devMode          = false
  -no2kSize         = false
  -rtl              = <%= rtl %>
  -breadcrumbs      = false
  -styles           = true
  -js               = pageType


block content

  // Content -----------------------------------------------------------------------------------------------------


  // Account
  +b.account__wrapper
    +e.body
      +addTestText(`${lotteryName}-test-text`)
  // Account



  // Account Tickets
  +b(class=`${lotteryName}-account__wrapper--logged`)
    +e.body
      +addTestText(`${lotteryName}-test-text`)
  // Account Tickets


  // Content -------------------------------------------------------------------------------------------------------





  // Addition Buttons ----------------------------------------------------------------------------------------------
  +addAditions(
  pageType,
  '#index',
  <%_ if (raffle) { -%>
  '#raffle',
  <%_ } -%>
  )
  // Addition Buttons ----------------------------------------------------------------------------------------------
